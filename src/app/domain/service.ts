export interface Service {
    serviceId?: string;
    name?: string;
    imageUrl?: string;
    price?: number;
    category?: string;
}

export interface ServiceInfo {
    name: string;
    description: string;
    imageUrl: string;
    category: string;
}