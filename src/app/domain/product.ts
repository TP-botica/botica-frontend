export interface Product {
    productId?: string;
    name?: string;
    imageUrl?: string;
    price?: number;
    category?: string;
    stock?: number;
}

export interface ProductOption{
 id: string;
 name: string;
}