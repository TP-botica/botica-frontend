export interface Product {
    productId?: string;
    name?: string;
    imageUrl?: string;
    price?: number;
    category?: string;
    stock?: number;
}

export interface Option{
 id: string;
 name: string;
}